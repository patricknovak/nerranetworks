openai>=3.6.0
xai-sdk>=1.19.0
yfinance>=1.7.0
python-dotenv>=1.2.3
tweepy>=4.17.0
requests>=2.34.2
tenacity>=9.1.4
feedgen>=1.0.0
beautifulsoup4>=4.15.0
feedparser>=6.0.14
Pillow>=12.3.0
# QR on the long-form outro card (engine/promo_card.py) — pure-python;
# the card renders without a QR when this is absent (best-effort).
qrcode>=8.2
websockets>=17.0.1
pytest>=9.1.1
pyyaml>=6.0.3
jinja2>=3.1.6
boto3>=1.43.89
faster-whisper>=1.2.1
google-api-python-client>=2.199.0
google-auth>=2.56.3
google-auth-oauthlib>=1.4.1
google-auth-httplib2>=0.4.2
# Unofficial Spotify for Podcasters analytics (scripts/fetch_spotify_stats.py)
spotifyconnector>=0.8.3
# Unofficial Apple Podcasts Connect analytics (scripts/fetch_apple_stats.py).
# Reads the same endpoints the Podcasts Connect dashboard uses, via
# myacinfo/itctx session cookies, which expire in roughly a day.
#
# Correction (July 2026): an earlier note here claimed "Apple ships no
# creator analytics API". That is wrong. Apple's Reporter command-line
# tool exposes apShowListening / apEpisodeListening / apProviderListening
# reports against a 180-day access token — no cookies.
#
# Tested end to end 27 July 2026: the token authenticates and the service
# is up, but the podcast provider account has no reporting vendor,
# because the Apple Podcasters Program agreement reads "Pending User
# Info". So this cookie scraper stays primary for now. Full findings and
# the retry procedure are in docs/analytics.md — read that before
# spending another afternoon on Reporter.
appleconnector>=0.4.1
