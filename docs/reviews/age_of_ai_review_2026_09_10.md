# age_of_ai — quality review (2026-09-10)

_Generated by the Grok-4.3 Show Review Agent (`scripts/run_show_review.py`). Proposed prompt/audio changes are listed in the PR body under "A/B-listen required" and are NOT applied — the operator applies and listens before merging (landmine #17)._

## Evidence

- **First recorded review** — `docs/reviews/ledger/age_of_ai.yaml` does not exist yet; no prior predictions to carry as open misses.
- **Deterministic snapshot** (`scripts/review_snapshot.py age_of_ai`): `digests/age_of_ai` has **0** `_tts.txt` scripts, **0** chapter files, **no** credit-usage files, **no** OP3 series data. Cross-episode boilerplate detection is empty by lack of substrate, not by proof of clean audio.
- **Production path (canonical, CLAUDE.md + YAML header):** AOAI is **not** a `run_show.py` show. Live episodes come from Nerra Voices (`pipelines/voices/`, `nerra_voices_*.yml`, Voximplant scenario, dual human gates). `shows/age_of_ai.yaml` is registry/publish only; `narrative_mode: true` + permanently empty topic queue makes accidental `run_show.py age_of_ai` a clean `narrative_queue_empty` skip before spend.
- **Why the snapshot is structurally wrong for this show:** Standard review ears are `_tts.txt` + run_show Whisper. AOAI ships **real guest PSTN audio + Mira narration** (`tts.voice_id: ara`), with per-channel Whisper and editorial package chapters (`02_chapter_markers` / `produce_episode.py` per CLAUDE.md). Absence of `_tts.txt` is **expected**, not evidence of “no show.” The harness therefore cannot score listener-facing audio quality, tics, length, or chapter shape for the path that actually publishes.
- **YAML registry surface (verified):** `shows/age_of_ai.yaml` — `memory_enabled: true`, `x_enabled: false`, `newsletter.enabled: false`, `youtube.enabled: false`, `multilingual.enabled: false`, RSS/site fields populated, `host_name: Mira`, `spotify_show_id: 033UC9Qevscuf6mFiuu396`, `apple_show_id: 6794048682`, contingency `llm.*_prompt_file` pointers, chapter `section_markers` explicitly scoped to contingency only.
- **Contingency prompts (verified):** `shows/prompts/age_of_ai_{system,digest,podcast}.txt` all open with production-notes that Voices owns live episodes. System prompt absolute fidelity rules (no invented guest words; disclosure every episode) match the show premise. Podcast prompt correctly bans voice-direction tags and phonetic respellings (landmine #17-aligned).
- **Cold-open contradiction on contingency path (verified):** Network cold-open work put `{hook}` before `{intro_line}` and injects `{delivery_spec}`. AOAI podcast prompt still orders identity line first, then premise, then hook — while also including `{delivery_spec}` at the top:
  - `shows/prompts/age_of_ai_podcast.txt` — `{delivery_spec}` then step 1 copies `{intro_line}` before `{hook}`.
- **No ledger `do_not_retry` / no closed review-PR signal** — nothing to honor or add from operator rejections this pass.
- **Not in evidence this pass (do not invent):** `pipelines/voices/**` source, Voximplant scenario body, Supabase rows, live RSS item count, guest-approval redaction assembly, or any shipped episode transcript. Those are the real quality surface and must be the substrate of the next pass.

## Scoring prior predictions

| Metric | Verdict | Evidence |
|--------|---------|----------|
| _(none — first ledger entry)_ | n/a | No `docs/reviews/ledger/age_of_ai.yaml` |

## P0 — listener-facing bugs shipping today

**None verified.** No committed episode scripts, Whisper transcripts, chapter JSON, or RSS enclosure sample were in the review bundle, and this agent cannot fetch live feeds or R2. Spotify/Apple catalog IDs are present in YAML, which makes an empty or stalled public feed *plausible* operator risk, but empty local `_tts.txt` is **not** proof of a broken subscriber feed for this architecture.

## P1 — quality ceiling

### P1.1 — Review harness blind to Nerra Voices artifacts (blocks every future AOAI pass)

**Evidence:** Snapshot reported 0 scripts / 0 chapters / no costs / no OP3 while CLAUDE.md describes a full Voices publish path (R2, mix, editorial passes, `engine.publisher`, waveform video). AOAI will keep looking “empty” under `_tts.txt`-centric tooling even after real episodes ship.

**Fix class (code/harness, not prompt):** Teach `scripts/review_snapshot.py` (and the review playbook’s Phase-1 list for this slug) to read AOAI’s real published substrate when present, e.g. `digests/age_of_ai/summaries_age_of_ai.json`, committed editorial/chapter JSON beside publishes, narration scripts or per-channel transcripts under the Voices output layout, and credit/metrics the Voices workflows actually write. Until that exists, every AOAI “quality pass” can only re-audit the registry shell.

**Prediction:** next snapshot for `age_of_ai` reports a non-“(none found)” episode/artifact section **or** an explicit `voices_pipeline: zero_published_episodes` line — never a false “0 _tts.txt” cleanliness story.

### P1.2 — Contingency podcast prompt fights network cold-open + `{delivery_spec}`

**Evidence:** `shows/prompts/age_of_ai_podcast.txt` includes `{delivery_spec}` (shared cold-open rules: substance first) then instructs:
1. Begin with exact `{intro_line}`
2. Then premise + `{hook}`

CLAUDE.md network note: prompts put `{hook}` before `{intro_line}`; `voice_intro_delay` 0 / short identity line. Contingency path is supposed to be unreachable while the topic queue stays empty, but if anyone hand-queues a topic the script stage gets contradictory ordering (greeting/identity before hook) and risks the pre-cold-open retention failure mode the network already paid for.

**Proposal:** Reorder contingency body to hook → exact `{intro_line}` → AI-host premise → brief narration → disclosure + `{closing_block}`. Keep verbatim fidelity rules. **A/B-listen required** if this path is ever exercised; no claim of audio verification in this pass.

### P1.3 — Do not treat missing `_tts` boilerplate stats as a quality win

**Evidence:** Snapshot “no cross-episode repeated phrases above threshold” with **n=0 scripts**. Filing tic cleanups or length floors from that number would be false confidence.

**Rule for next passes:** Score tics from Voices guest+Mira transcripts / narration text only; never from empty run_show paths.

## P2 — growth / discoverability

### P2.1 — Distribution still launch-gated (by design)

**Evidence:** `publishing.x_enabled: false`, `newsletter.enabled: false`, `youtube.enabled: false`, multilingual off — matches CLAUDE.md “RSS + site only at launch; phase-8 public launch.” Waveform video is pipeline-owned independent of `youtube.enabled`.

**Not a defect.** Carry phase-8 enable as deferred operator launch work, not a drive-by YAML flip.

### P2.2 — Catalog IDs set while local review substrate is empty

**Evidence:** `spotify_show_id` / `apple_show_id` populated in `shows/age_of_ai.yaml`; snapshot still has no OP3 series row for the show.

**Operator check (no code proposed):** Confirm Apple/Spotify episode counts and OP3 feed resolution against `age_of_ai_podcast.rss`. If catalogs are live with zero items, fix publish/submit before marketing; if episodes exist only remotely, fix harness ingestion (P1.1).

### P2.3 — Memory enabled on a non-run_show show

**Evidence:** `memory_enabled: true` with note that production memory is Voices `episode_memory_block`, not run_show hooks; registered in `SHOW_MEMORY_CONFIGS` (CLAUDE.md July 24 2026).

**Risk:** Empty or stale narrative/Story Tracker page if publish does not write the same memory surfaces other shows use. **Defer** to a Voices-memory coherence check once episode artifacts are visible — do not dual-write from contingency prompts.

## What would have “shipped” in an interactive agent session

This pass runs **propose-only** (non-interactive). Nothing is applied. No tests executed here. No prompt/audio change should merge without operator A/B-listen if the contingency path is ever used.

## Deferred

- Full Nerra Voices pipeline audit (apply→triage→Cal.com→live call→gates→assembly→publish) with real episode transcripts and editorial JSON.
- Phase-8 X / YouTube / newsletter / multilingual public launch.
- Any podcast-side word-floor / expand-retry length lever (network meta-review: length misses; interview length is guest- and edit-driven).
- Phonetic respellings, speech-tag injection, TTS voice_id / provider edits (landmine #17 — 100% regression class).
- Partial flips of coupled TTS wrap fields; R2 path / enclosure URL changes; `min_articles_skip` default edits.
- Guest-mix / diversity dashboarding and dispatch-funnel conversion metrics (needs Supabase/Voices analytics access).
- OP3/download growth work until feed resolution and episode count are confirmed.

## Guardrails honored

No R2/RSS enclosure rewrites, no git LFS/MP3 commits, no voice_id changes, no run_show forced generation, no claim that contingency prompt edits are audio-verified, no length pressure on the podcast stage, no re-proposal of landmine #17 speech hacks.
