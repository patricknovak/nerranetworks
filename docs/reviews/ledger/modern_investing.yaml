# Review ledger for modern_investing. Schema: docs/reviews/ledger/README.md
# Created by the 2026-07-02 network editorial pass (first scored MIT entry;
# the June 2026 flagship pass predates the ledger system for this show).
reviews:
  - date: 2026-07-02
    pr: null
    doc: docs/reviews/network_review_2026_07_02.md
    reviewer: claude-code (network editorial pass)
    summary: >
      Network editorial pass — first ledger entry. Most actionable show in
      the network, but its worst-possible error class shipped: PHANTOM
      (data-failure) trades narrated as market outcomes ("ROKU closed
      flat…", "KO closed flat today" — no price was ever fetched; 4 of the
      spoken "7 breakeven outcomes" were data failures). The MU trade was
      reviewed as "yesterday's flash trade" three times. Also: the seeded
      callback sentence colonized 9/10 episodes; two irreconciled alphas
      spoken (+11% daily vs -13.1% Sunday — the Sunday honesty is a strength
      to keep); Ep093 amplified an investing.com promo headline ("Qualys
      surged 65%") with invented same-day framing (operator item).
    shipped:
      - voided-trade status for data-failure closes — excluded from every
        aggregate / review / lookback (deterministic)
      - one-time tracker migration to the honest 37-trade record (was 41
        with 4 phantoms)
      - review-once guard (each closed trade reviewed exactly once)
    deferred:
      - "A/B proposals (NOT applied): de-seed the 'This is the exact
        scenario where our earlier rule…' callback; retire 'closing-price
        confirmation' as a teachable lesson (it describes the pipeline's own
        former price-fetch bug); label the two alpha metrics whenever
        spoken; Saturday scripts say 'Friday's close'"
      - "operator: demote the investing.com promo tier / require a second
        source for >20% single-day move claims"
    predictions:
      - metric: spoken closed-trade record vs tracker after migration
        baseline: "41 spoken incl. 4 phantom data-failure 'breakeven' trades"
        expected: "spoken record excludes voided trades (37 honest trades);
          no data-failure close ever narrated as a market outcome"
        verdict: partial
        evidence: "2026-07-03 review: the 4 null-price trades are voided and
          excluded, but the DELL Ep57 / HIMS Ep63 NaN-exit closes were missed
          by the migration and stayed status=closed with pnl=NaN — 2 of the 3
          spoken 'breakeven' trades were still data failures. Fixed via the
          self-healing _void_nonfinite_closed_trades migration."
      - metric: times a single closed trade is reviewed on air
        baseline: "MU reviewed as 'yesterday's flash trade' 3x"
        expected: "exactly once per closed trade (review-once guard)"
        verdict: hit
        evidence: "Ep94 reviewed MU once (reviewed_in_episode=94), Ep95
          reviewed GIS once (95); no re-narration in Ep94/95 transcripts."
    agent_cost_usd: null

  - date: 2026-07-03
    pr: null
    doc: docs/reviews/modern_investing_review_2026_07_03.md
    reviewer: claude-code (/review-show, operator-requested live-trading focus)
    summary: >
      Benchmark-integrity pass driven by the operator's intent to connect
      the strategy to a live account. The measurement layer was not
      measuring what it claims: every flash trade had nasdaq_return_pct
      0.0 (alpha = raw return), mid-week weekly picks were backdated to
      Monday's open (hindsight gain, Ep35 AMD +13.36%), Ep50 CNR was
      priced as the wrong US company (Core Natural Resources, not
      Canadian National Railway), DELL/HIMS NaN closes were still spoken
      as breakevens, --test runs appended real trades, and the
      lessons_learned "recursive learning loop" had degenerated into an
      echo chamber (65 actives, ~47 copies of two rules; the
      closing-price tic in 9/10 transcripts). Live-trading verdict: NOT
      READY — honest record is +$304 over 40 sequential $1k trades vs
      NASDAQ +15.46% ITD; run scripts/recompute_mit_benchmarks.py for the
      first trustworthy alpha baseline, then accumulate 2-3 months of
      clean record.
    shipped:
      - matched-bar-window NASDAQ benchmark (index open→close over the
        trade's own bars; flash windows no longer zero)
      - pick-date entry integrity (no bars before the pick; cron-delay
        wrong-day pricing killed; stale picks void after 10d)
      - TSX/.TO symbol resolution + pick-time validation probe +
        price-discontinuity tripwire
      - self-healing void of non-finite closed trades + tracker migration
        (40 closed / 6 voided; breakeven 3→1; win rate 57.5%)
      - NERRA_HOOKS_READONLY guard (--test/--rehearse never mutate state)
      - lessons_learned dedup-on-append + diverse 5-rule block + ledger
        collapse 65→11 distinct actives
      - matched-window compounded alpha in summary + labeled scoreboard
        blocks (A/B-listen)
      - honest bar-day close labels ("Thursday close"); calibration
        reports all confidence buckets; monthly-snapshot dead key fix
      - scripts/recompute_mit_benchmarks.py (operator runs with market
        access; realigns historical windows, reports wrong-instrument
        trades)
      - drift guards tests/test_mit_benchmark_integrity.py (37 tests);
        brittle total_trades==37 pin replaced with invariants (was red on
        main)
    deferred:
      - "operator: run scripts/recompute_mit_benchmarks.py --apply and
        adjudicate its wrong-instrument report (Ep50 CNR expected)"
      - "retire the closing-price-confirmation lesson family
        (LL-054/LL-005/LL-013 — pipeline bugs taught as investing
        lessons); one-line status flip after the dedup; operator A/B call
        from July 2"
      - "chronic under-length (8/10 below 1800w floor) — digest ceiling,
        behind the four-show length A/B"
      - "investing.com promo-headline demotion (operator, July 2)"
      - "close weekly holds on Saturday's run to capture the true Friday
        close (cadence decision)"
      - "recap/weekend episodes creating trades from stale news — pricing
        now fair (Monday entry), but consider disabling picks on recaps"
      - "extend NERRA_HOOKS_READONLY honoring to other state-mutating
        hooks (tesla/spacex/FF/PT memory, PR vocab)"
      - "execution-cost modeling (spread/commission/FX) + enforced stops
        before any live-account decision"
    predictions:
      - metric: closing-price-confirmation phrase frequency (snapshot
          repeated-phrases, last 10 transcripts)
        baseline: "9/10 episodes"
        expected: "<= 2/10 within 10 episodes (dedup removed the echo; the
          rule is 1 of 11 distinct actives, no longer 5-of-5 in the block)"
        verdict: hit
        evidence: "2026-07-18: 0/10 in the last-10 snapshot (was 9/10); ledger stable at 11 distinct actives, no echo regrowth"
      - metric: flash trades with nasdaq_return_pct == 0.0
        baseline: "8/8 flash trades"
        expected: "0 new flash trades with a zero benchmark window"
        verdict: hit
        evidence: "2026-07-18: no flash trades occurred; both new closes (COST/DAL) carry real matched windows — thin n, re-score when a flash closes"
      - metric: weekly trades with entry_bar_date < pick date
        baseline: "all mid-week weekly picks backdated to Monday"
        expected: "0 new trades with entry before pick date"
        verdict: hit
        evidence: "2026-07-18: Ep101 COST and Ep102 DAL entry bars equal their pick dates"
      - metric: TSX picks priced on the .TO listing
        baseline: "0 (bare US symbols; Ep50 CNR wrong company)"
        expected: "every new TSX pick carries resolved_symbol ending .TO/.V
          or a loud PICK VALIDATION FAILED log"
        verdict: pending
        evidence: "no TSX picks in the window yet; both US picks carry resolved_symbol"
    agent_cost_usd: null

  - date: 2026-07-04
    pr: null
    doc: docs/mit_snaptrade_live_trading_plan.md
    reviewer: claude-code (operator-requested optimize-the-loop pass)
    summary: >
      Follow-on to the July 3 benchmark-integrity pass, aimed at "beat all
      major indices" + a learning loop that audits itself. Multi-index
      matched-window benchmarking (^IXIC/^GSPC/^GSPTSE per trade,
      benchmark_scores + indices_beaten in summary, index sweep in the
      scoreboard block); rule-effectiveness scoring (trades stamped with
      rules_in_effect; stamped-vs-without alpha per rule; >=8-trade
      no-edge rules flagged as retirement candidates, operator-decided);
      shadow mode (Phase 2) shipped and LIVE with no secrets needed
      (risk gates + would-be marketable-limit orders in a committed
      shadow ledger at 13:50 UTC weekdays; mit_shadow_report.py =
      sim-vs-shadow slippage).
    shipped:
      - multi-index matched-window benchmark_returns + benchmark_scores +
        indices_beaten (recompute script rebuilds all three for history)
      - rules_in_effect stamping + rule scoreboard with retirement
        candidates (never auto-retired)
      - execution/risk.py hard gates + execution/shadow.py +
        mit-shadow-executor.yml (weekdays 13:50 UTC) +
        scripts/mit_shadow_report.py
      - shadow EXITS (Phase 2.5, same PR) — would_sell entries on the
        sim's exit calendar, idempotent, round-trip shadow_return_pct;
        report gains the shadow-vs-sim P&L gap per trade
      - regime block (rolling last-10 alpha + drawdown from high-water
        mark → cold streak raises the pick bar / hot streak holds
        discipline; rides in the strategy_performance slot)
      - statistical discipline — FAVOR/AVOID and operating principles now
        require n>=5 closed trades per sector/tag (was 2-3, coin-flip
        samples were steering picks); small samples labeled inconclusive
      - performance page — matched-window alpha tile (honest headline),
        buy-and-hold gap relabeled "not capital-matched", major-index
        sweep row
    deferred:
      - "feed measured shadow slippage into the sim's cost model once ~20
        matched round trips accumulate"
      - "webhook receiver Worker for SnapTrade signature verification
        (Phase 2 scope if wanted)"
      - "carried: closing-price lesson retirement (operator A/B),
        under-length (four-show A/B), recompute --apply run (operator)"
    predictions:
      - metric: shadow ledger entries after 2 weeks of weekday runs
        baseline: "0 (ledger created this pass)"
        expected: ">= 8 entries with decision in {would_place, skipped};
          zero duplicate client_order_ids; workflow green without secrets"
        verdict: hit
        evidence: "2026-07-18: 12 entries covering every weekday, entries + paired exits, zero duplicate ids, workflow green with no secrets"
      - metric: closed trades carrying benchmark_returns for all 3 indices
        baseline: "0 of 40"
        expected: "every NEWLY closed trade has nasdaq+sp500+tsx returns
          (history backfills when the operator runs the recompute script)"
        verdict: hit
        evidence: "2026-07-18: both new closes carry nasdaq+sp500+tsx; history still awaits the operator recompute"
      - metric: closed trades stamped with rules_in_effect
        baseline: "0 of 40"
        expected: "every new pick stamped; first rule scoreboard lines
          appear after ~5 closes; retirement candidates only at >=8"
        verdict: hit
        evidence: "2026-07-18: both stamped with the exact 5 rule IDs; scoreboard activates at ~5 stamped closes"
    agent_cost_usd: null

  - date: 2026-07-04
    pr: null
    doc: docs/mit_snaptrade_live_trading_plan.md
    reviewer: claude-code (fidelity batch, operator "continue improvements")
    summary: >
      Sim-fidelity + statistical-honesty batch. (1) Stop-loss enforcement:
      the digest narrates a stop on every pick but the sim never enforced
      it — stops are now parsed at pick time (never guessed), stored on
      the trade AND in the trade signal (future bracket orders), and
      enforced at evaluation against intraday lows (gap-aware fills;
      entry-bar breaches never claimed; benchmark window follows the
      shortened hold). (2) Per-trade alpha t-stat in the summary; the
      scoreboard block instructs the model to hedge on air when the edge
      is not yet statistically distinguishable from luck. (3) Dashboard
      execution_health: signal freshness/staleness + shadow-ledger vitals
      (decision counts, round trips, avg round-trip).
    shipped:
      - _extract_stop_loss + _stop_breach + enforcement in _close_trade;
        stopped_out narration in the trade review; stop_loss in the trade
        signal schema
      - bars carry intraday lows (4th element, None-safe for legacy)
      - alpha_t_stat + alpha_statistically_significant in summary +
        honesty line in the benchmark block
      - execution_health block in aggregate_mit_performance
    deferred:
      - "same-day (entry-bar) stop breaches — unknowable from daily bars;
        revisit with intraday data if ever needed"
      - "carried: slippage->sim cost model (~20 shadow round trips),
        recompute --apply (operator), closing-price lesson retirement
        (operator A/B)"
    predictions:
      - metric: trades with a parsed stop_loss on the trade record
        baseline: "0 of 46 (never captured)"
        expected: ">= 70% of new picks carry stop_loss (the Risk
          Assessment states one nearly every episode); no trade ever
          carries a stop above its entry"
        verdict: hit
        evidence: "2026-07-18: 2/2 new picks carry {pct: 6.0}; none above entry"
      - metric: spoken outperformance claims without a significance hedge
        baseline: "n/a (t-stat never computed)"
        expected: "while alpha_statistically_significant is false, episodes
          claiming outperformance also say it is early/not yet significant"
        verdict: miss
        evidence: "2026-07-18: alpha quoted in most episodes, hedge spoken in ZERO — separate-instruction design failed; fixed by embedding the caveat inline in the alpha sentence"
    agent_cost_usd: null

  - date: 2026-07-04
    pr: null
    doc: docs/mit_snaptrade_live_trading_plan.md
    reviewer: claude-code (operator "build the next layer")
    summary: >
      Phase 3 live executor shipped DORMANT. execution/live.py places
      integer-share marketable-limit BUYs from the trade signal and SELLs
      on the sim's exit calendar, behind ordered fail-closed gates: kill
      switch (repo var LIVE_TRADING_ENABLED; unset = no-op that never
      loads credentials) -> SnapTrade config -> self-halt (2 consecutive
      rejects; exits still run while halted) -> shared risk gates ->
      daily/open-position caps -> account resolution -> live quote.
      Idempotent client_order_id; unfilled Day entries mark flat instead
      of shorting; every action notifies. Privacy: committed
      live_execution_state.json is id/status-only; full live_ledger.json
      gitignored + CI artifact. Phase-1/2 no-placement contract
      superseded by a narrower pinned one (SDK call only in the wrapper,
      wrapper called only by live.py, kill switch first — poisoned-client
      test).
    shipped:
      - execution/live.py (run_live_entry/run_live_exits + halt logic)
      - snaptrade_client.place_limit_order/order_status (single SDK
        trading call site)
      - scripts/mit_live_executor.py + mit-live-executor.yml (13:52
        entries / 19:45 exits slots)
      - contract re-pin + 13 live-layer drift guards
    deferred:
      - "arming is the OPERATOR's call, gated on the shadow record + clean
        sim record per the plan; set 4 secrets + LIVE_TRADING_ENABLED=1"
      - "v1.1: realized daily-loss halt (needs fill polling), native
        bracket/stop attachment (broker-matrix dependent), sim-vs-live
        divergence alarm"
    predictions:
      - metric: mit-live-executor workflow runs while dormant
        baseline: "workflow new"
        expected: "every scheduled run green with 'live layer dormant'
          no-op; zero SnapTrade calls until the operator arms it"
        verdict: hit
        evidence: "2026-07-18: no live_execution_state.json exists — the dormant layer has never touched anything"
    agent_cost_usd: null

  - date: 2026-07-18
    pr: null
    doc: docs/reviews/modern_investing_review_2026_07_18.md
    reviewer: claude-code (operator-requested two-week scoring review)
    summary: >
      First full scoring pass over the July 3-4 overhaul: 8 predictions
      HIT, 1 MISS, 1 pending. The machinery works (signals every episode,
      shadow ledger complete with paired exits, stops parsed 2/2, rules
      stamped, tic eliminated, live layer untouched) — but two design
      flaws surfaced in production: the regime cold-streak brake
      DEADLOCKED the Practice Investment segment (2 picks in 14 episodes;
      mean poisoned by the -11.8 MDA outlier + $100 drawdown threshold
      permanently tripped at $164 standing + cold suppresses the very
      closes that refresh the window), and a Thursday weekly pick closed
      as a degenerate one-bar "weekly hold" (Ep101 COST, entry==exit bar,
      -2.35% while the shadow layer holding Thu->Fri made +0.26% on the
      same pick). The significance hedge never reached air (alpha quoted
      most episodes, hedge spoken zero) — separate-instruction design
      failure. Index sweep was comparing 37-trade vs 2-trade samples.
    shipped:
      - regime v2 — median (outlier-robust), drawdown threshold $100->$250
        (full position), pick-drought escape valve (>7 days cold ->
        SELECTIVE RESET expecting the next Monday pick), cold-streak
        on-air transparency instruction
      - weekly min-hold — Friday close requires >=2 days since pick
        (Thu/Fri picks roll to next Friday); shadow _exit_due_date matches
      - index sweep gated on n>=5 per index, 2+ qualifying
      - significance caveat embedded INLINE in the alpha sentence
      - no-trade reason taxonomy (explicit None/No Trade/Mid-Week forms;
        no_practice_section for recaps; no_pick_extracted = real drift)
    deferred:
      - "watch: 'volume above the 20-day average' spoken 8/10 (LL-017
        echo); rule scoreboard adjudicates at ~5 stamped closes"
      - "product: performance-page execution-reality section at ~20 shadow
        round trips; Sunday weekly-scoreboard ritual"
      - "operator: recompute --apply still not run (current +6.59% matched
        alpha retains old-window inflation; sweep suppressed until
        backfill); SnapTrade Phase-0 status unverifiable from repo;
        closing-price lesson retirement still open"
      - "carried: under-length (digest ceiling); shadow executor cron
        delays (runs 15:04-16:48 UTC vs 13:50 slot, landmine #24 class)"
    predictions:
      - metric: new picks per week (trade signals with action=new_trade)
        baseline: "1/week over Jul 6-18 (both Monday weeklies suppressed)"
        expected: ">=1 Monday weekly pick per week under regime v2; no
          pick drought longer than 7 days while SELECTIVE RESET is
          reachable"
        verdict: partial
        evidence: "2026-07-24: the escape valve produced a weekly pick 3 days
          after v2 merged (Jul 21, ending an 11-day mostly-pre-fix drought) —
          but on TUESDAY (Mon Jul 20 was an explicit no-trade), and the pick
          was lost to the suffixed-symbol extraction bug (BTC-USD -> 'BTC',
          wrong instrument, voided)."

      - metric: weekly holds with entry_bar_date == exit_bar_date
        baseline: "1 (Ep101 COST, the Thursday degenerate)"
        expected: "0 new one-bar weekly holds"
        verdict: pending
        evidence: "2026-07-24: no weekly hold closed in the window (the only
          new pick voided on the wrong-instrument tripwire) — carry forward."

      - metric: episodes quoting the matched-window alpha with the
          significance qualifier in the same breath
        baseline: "0 of ~8 alpha mentions (hedge separate -> dropped)"
        expected: "inline caveat echoed in >=70% of alpha mentions while
          t<2"
        verdict: miss
        evidence: "2026-07-24: post-merge alpha mentions Ep111 0/1, Ep112 1/2,
          Ep113 0/1, Ep114 0/2 = 1/6 (17%) — the em-dash instruction form is
          stripped during paraphrase. Second miss on this metric; third
          mechanism shipped (caveat fused into the alpha VALUE as a
          data-shaped parenthetical); a third miss goes to the operator as
          accept-or-abandon."

    agent_cost_usd: null

  - date: 2026-07-24
    pr: null
    doc: docs/reviews/modern_investing_review_2026_07_24.md
    reviewer: claude-code (/review-show, operator-directed out of rotation)
    summary: >
      Operator-reported issues in latest episodes / source referencing /
      pick reviews / performance reporting — all four verified. P0: the
      extractor could not parse the exchange-native symbols the July-3
      pass taught the digest to emit — Ep111's spoken CNR.TO weekly pick
      was silently LOST (signal: no_pick_extracted) and Ep113's
      "BTC-USD — Bitcoin" was truncated to "BTC", validated against an
      unrelated equity at $28.80 (narrated stop $64,500 — above entry),
      routed to Webull in the trade signal, and would_place'd by the
      shadow executor at $29.62: the wrong instrument reached the
      execution layer. P0: Ep115 fetched 9 articles (vs 222-337 median)
      and shipped 6/6 x.com sources incl. a duplicate unusual_whales URL
      + the Tools header scaffold typo rendered verbatim ("...Tool:
      Source"). P1: alpha quoted without the significance caveat (miss
      #2), chronic under-length (10/10 below floor, median ~1382w),
      "no newly closed trade" in 9/10 episodes (drought + lost picks).
    shipped:
      - suffixed/hyphenated symbol extraction (CNR.TO / BTC-USD / .V) +
        CRYPTO market recognition + TSX-V alternation-order fix
      - crypto candidates force the -USD Yahoo pair with NO bare
        fallback; pre-suffixed symbols pass through verbatim
      - "wrong-instrument tripwire — stop outside [ref/3, ref*3] voids
        the pick at record time (instrument_scale_mismatch), writes an
        explicit no-trade signal, + self-healing migration (committed
        tracker migrated: Ep113 BTC voided)"
      - void transparency — an announced-then-voided pick is disclosed
        on air exactly once via the trade-review block
      - "source-collapse alarm in run_show (article_count_degraded +
        a GitHub warning annotation when count < 25% of the show's recent
        median; log-only, all shows)"
      - digest-substrate length lever — digest_expand_below_target true,
        min_digest_words 1500 (A/B)
      - digest prompt (A/B) — x.com = one publication / publisher-first
        sourcing / thin-day depth rule; Tools header scaffold typo fixed;
        Today's Pick template instructions moved out of the value slot
        (Ep111 echoed the parenthetical on air) + explicit ticker format
      - alpha caveat mechanism 3 (A/B) — qualifier fused into the alpha
        value as a data-shaped parenthetical
      - 18 new drift guards in tests/test_mit_benchmark_integrity.py
    deferred:
      - "operator: Ep31 'BTC' flash (April, closed +0.42% @ $33.67) is
        the same wrong-instrument class historically; no stop recorded so
        the tripwire cannot catch it — adjudicate via recompute report"
      - "operator: Ep111 lost CNR.TO pick was spoken on air but never
        recorded; optional one-line on-air acknowledgment"
      - "carried: recompute --apply (matched alpha retains old-window
        inflation), SnapTrade Phase-0 verification, closing-price lesson
        retirement, shadow-executor cron delay (landmine #24 class)"
    predictions:
      - metric: suffixed/crypto picks recorded correctly (tracker symbol
          matches the digest's Today's Pick symbol verbatim)
        baseline: "Ep111 CNR.TO lost; Ep113 BTC-USD truncated to BTC"
        expected: "0 lost or mangled suffixed picks; every crypto pick
          resolves to the -USD pair"
        verdict: hit
        evidence: "2026-08-15: Ep133 IPCO.TO and Ep135 X.TO both recorded
          with the suffix intact and resolved_symbol matching; no pick lost
          to no_pick_extracted in the window. No crypto pick occurred, so
          the -USD half is untested — carried into the next entry."
      - metric: non-voided trades with stop/reference ratio outside
          [1/3, 3]
        baseline: "1 (Ep113 BTC, open, stop 2240x reference)"
        expected: "0 — the tripwire voids at record time and the
          migration self-heals any that slip through"
        verdict: hit
        evidence: "2026-08-15: 0 across all 50 non-voided trades; the Ep113
          BTC pick remains voided with instrument_scale_mismatch."
      - metric: alpha mentions carrying the significance qualifier
          (while t<2)
        baseline: "1/6 (17%) with the em-dash instruction form"
        expected: ">=70% with the data-shaped parenthetical; a third
          miss escalates to an operator accept-or-abandon decision
          instead of a fourth mechanism"
        verdict: miss
        evidence: "2026-08-15: 0 of 22 alpha mentions across Ep126-137
          carried any significance hedge. THIRD miss on this metric; per
          the playbook no fourth mechanism was written. Escalated to the
          operator as accept-or-abandon (see the 2026-08-15 review).
          Confound worth noting before abandoning: the alpha figure being
          quoted was the blended 45-trade number, which the 08-15 pass
          replaced with a verified-window figure that carries its own
          sample size inside the sentence — re-score once that has run."
      - metric: median script words, last-10 snapshot
        baseline: "~1382w (all 10 below the 1800 floor)"
        expected: ">=1550w within 10 episodes of the digest retry
          shipping (digest-substrate lever, min_digest_words 1500)"
        verdict: partial
        evidence: "2026-08-15: median 1535w over Ep128-137 (1382 -> 1535,
          +11%) but short of the 1550 target and 9 of 10 still below the
          1800 floor. The digest substrate moved and then plateaued, which
          points at the script stage — hence the grok-4.6 script-model A/B
          (experiment mit-script-model-46)."
      - metric: episodes with >2 x.com Source citations on a
          normal-fetch day (article_count > 100)
        baseline: "Ep115: 6/6 x.com (collapsed-fetch day)"
        expected: "0; collapsed-fetch days additionally emit
          article_count_degraded"
        verdict: miss
        evidence: "2026-08-15: 6 of the last 12 episodes exceed 2 x.com
          citations (Ep128 6/7, Ep131 5/8, Ep134 5/8, Ep136 5/8). The
          prediction's own >100-article precondition turned out to be
          unmeetable: MIT's fetch had collapsed from a median of 274
          (ep90-119) to 50-64 (ep120-137) because three RSS sources began
          returning 403, and the degradation alarm went silent as its
          rolling baseline decayed with the problem. Root cause fixed
          2026-08-15 (dead feeds replaced + long-baseline decay alarm);
          re-score against restored volume."
    agent_cost_usd: null

  - date: 2026-08-15
    pr: null
    doc: docs/reviews/modern_investing_review_2026_08_15.md
    reviewer: claude-code (operator-directed pipeline/data/learning-loop pass)
    summary: >
      Operator asked for a deep pass on the pipeline's data, analytics,
      recursive learning and goal adjustment. The mechanisms built by the
      July 3-24 passes all still work; what had failed was three loops
      whose correctness depended on data that moved underneath them, none
      of them visible from the dashboard or metrics. P0: the on-air
      headline alpha (+9.28% across 45 trades, spoken Ep137) was carried
      by the 35 trades whose benchmark windows the July-3 pass itself
      disowned — the 10 trades with pick-date-aligned windows were at
      -1.95% — because the operator recompute has never run. P0: the
      Portfolio Performance section was ABSENT from 13 of the last 20
      digests and the podcast stage filled the gap by telling listeners
      the performance feed was down (Ep134/135 verbatim); root cause was
      that the digest prompt's FORMATTING template, the part the model
      actually follows, never listed the section. P0: the July review-once
      guard only ever examined closed[-1], so once the pick cadence became
      roughly daily every close but the newest was skipped permanently —
      43 of 50 closed trades never narrated, five within the preceding ten
      days including MU's +5.12%, while their P&L still counted in the
      totals the segment reported. P1: "Weekly Hold" holds ran 0-6 days
      (median 3) against a five-day thesis, structurally, because exits
      are pinned to the Friday pre-market run which prices Thursday's bar.
      P1: article fetch had fallen 274 -> ~57 median for three weeks after
      CNBC x2 and Benzinga began returning 403, and the July-24 collapse
      alarm went quiet because its 10-episode rolling baseline decayed
      with the problem.
    shipped:
      - verified-window alpha (verified_window_alpha_pct / _trades /
        unverified_window_trades / verified t-stat) computed over
        entry_bar_date trades only; on-air headline switched to it with
        the trade count fused into the sentence and an explicit NOT-for-air
        line on the blended figure (A/B-listen)
      - "### Portfolio Performance added to the digest FORMATTING template
        (the actual root cause), plus an explicit ban in both digest and
        podcast prompts on narrating the performance data as unavailable
        or pending (A/B-listen)"
      - trade review drains the backlog oldest-first, one per episode,
        bounded by REVIEW_BACKLOG_MAX_DAYS=14; stale backlog retired in
        place; newest close never retired so an outage cannot silence the
        segment
      - "review block states the actual hold span (\"Actual hold: N
        calendar day(s), Monday -> Thursday\") with a ban on five-day
        framing the dates do not support (A/B-listen)"
      - source-decay alarm — second, longer baseline (last 10 vs the 50
        before) catches a slide as well as a cliff; both medians recorded
        as metrics; metrics paths sorted by episode number
      - three dead feeds (CNBC x2, Benzinga — hard 403 to the production
        UA) replaced with live probed feeds (Yahoo Finance, Google News
        earnings, Google News rates)
      - "llm.podcast_model: grok-4.6 — SCRIPT STAGE ONLY A/B (experiment
        mit-script-model-46); digest/fetch stay on grok-4.3 because 4.6 is
        post-trained on 4.5 and 4.5's confident-hallucination regression
        (25% -> 54%) is unmeasured for it, not disproven"
      - engine/generator.py falls back to config.llm.model with a loud
        warning when a script-stage override model is unreachable, so a
        bad id costs the experiment and never an episode
      - grok-4.6 pricing in engine/tracking.py
      - 16 new drift guards in tests/test_mit_benchmark_integrity.py
    deferred:
      - "operator: run scripts/recompute_mit_benchmarks.py --apply — open
        since 2026-07-03 and now the highest-value action on the show; it
        collapses the verified/legacy split and takes the honest alpha
        sample from n=10 to n=45"
      - "operator: accept-or-abandon on the significance qualifier (third
        miss, no fourth mechanism written per the playbook)"
      - "operator: weekly-hold cadence — fixed five bars from entry vs the
        current Friday-close calendar; the shadow executor's exit calendar
        would have to move with it"
      - "x.com source concentration — root cause (feed rot) fixed, but
        re-score against restored volume before adding a source-mix rule"
      - "carried: SnapTrade Phase-0 verification; closing-price lesson
        retirement"
    predictions:
      - metric: episodes whose digest contains a Portfolio Performance
          section
        baseline: "7 of the last 20 (13 missing)"
        expected: ">=18 of the next 20; ZERO episodes narrating the
          performance data as unavailable/pending/awaiting a tracker
          update"
        verdict: hit
        evidence: "2026-08-18: present in 3/3 post-merge episodes
          (Ep138/139/140) and zero outage claims, against 6 in the prior
          20. Thin n but the mechanism is a template line, so it either
          fires or does not — re-score at 20 episodes."
      - metric: closed trades never narrated on air (excluding the
          retired pre-2026-08-15 backlog)
        baseline: "43 of 50; 5 closed within the preceding 10 days"
        expected: "0 — the backlog drains one per episode and every new
          close is narrated exactly once"
        verdict: hit
        evidence: "2026-08-18: LNTH reviewed in Ep138, MU in Ep139, TBBK
          in Ep140 — exactly one per episode, oldest first. 38 stale
          trades retired without reaching air; 2 fresh closes remain
          queued (IPCO.TO, AAPL) and drain next."
      - metric: alpha figures spoken on air that come from the blended
          45-trade window
        baseline: "every alpha mention; Ep137 said '9.3% across 45
          benchmarked trades'"
        expected: "0; every spoken alpha is the verified-window figure
          and states its trade count in the same sentence"
        verdict: miss
        evidence: "2026-08-18: 2 of 3. Only Ep140 spoke the verified
          figure. The pass fixed _build_portfolio_summary and left
          _build_benchmark_block — the 'state every episode' scoreboard —
          on the blend, so both reached the same prompt and the model
          fused them: Ep138 aired '+9.28% across forty-five VERIFIED-window
          trades', the inflated number wearing the honest label, which is
          worse than the original bug. Fixed 2026-08-18; guarded by
          TestSingleAlphaSource."
      - metric: median article_count, last-10 snapshot
        baseline: "~57 (down from 274 over ep90-119)"
        expected: ">=120 within 10 episodes of the feed replacement; the
          decay alarm fires on any future slide below half the long
          baseline"
        verdict: partial
        evidence: "2026-08-18: volume did NOT recover (Ep138-140 at
          67/30/45) — one of the three replacement feeds (Yahoo Finance)
          was removed by the repo's blocked-source check, since a
          reachability probe cannot see an editorial ban. The alarm half
          of the prediction HIT: article_count_degraded fires on all three
          episodes, which is the correct reading. Deliberately no
          fetch-config change this pass: source COUNT does not track
          article volume (Ep139 cited 8 sources from 30 articles vs Ep138
          3 from 67), so the mechanism is not understood yet."
      - metric: median script words, last-10 snapshot
        baseline: "1535w (9 of 10 below the 1800 floor)"
        expected: ">=1700w under the grok-4.6 script stage, with zero
          numbers/tickers/prices in the script that are absent from the
          digest — a single invented figure reverts the experiment"
        verdict: hit
        evidence: "2026-08-18: Ep138-140 at 2256/2366/2078 words — all
          three clear the 1800 floor for the first time in the show's
          recent history, against 1441-1576 in the four episodes before.
          Factuality audit traced every spelled-out number in all three
          scripts to its digest (incl. $1.799T/$1.775T deficit figures,
          Anthropic $190-200B/$2T, Cerebras $842M); no invented tickers.
          Continue to the 08-29 readout. Cosmetic only: 4.6 said 'one
          trillion seven hundred ninety-nine billion dollars' twice."
    agent_cost_usd: null

  - date: 2026-08-18
    pr: null
    doc: docs/reviews/modern_investing_review_2026_08_18.md
    reviewer: claude-code (operator-directed impact review of the 08-15 pass)
    summary: >
      Three-day impact read on the August 15 changes, plus the first
      successful execution of the realignment dry run. Five of seven
      shipped items verified working; the grok-4.6 script stage is the
      clearest win the show has had on length (1441-1576w -> 2078-2366w,
      first episodes to clear the 1800 floor) with factuality intact.
      P0 REGRESSION found and fixed - the verified-alpha switch was
      applied to _build_portfolio_summary but not to _build_benchmark_block,
      the "state every episode" scoreboard; both reach the same prompt, so
      the model fused label and value and Ep138 aired "+9.28% across
      forty-five VERIFIED-window trades", the inflated number wearing the
      honest label. Fixing one of two call sites was worse than fixing
      neither. The index sweep had the same shape - its NASDAQ leg fell
      back to nasdaq_return_pct (45 trades) beside 10-trade S&P/TSX legs,
      printing "+9.28%" two sentences after the corrected "-1.95%"; the
      July-18 n>=5 gate missed it because it checks sample SIZE, not that
      the legs share the same TRADES. Separately, the recompute script
      that this ledger has recommended since 2026-07-03 - and that the
      08-15 review called the highest-value action on the show - would
      have CORRUPTED the record: it searches bars from 10 days before the
      pick and took the FIRST price match inside +/-2%, so it re-created
      the hindsight backdating it exists to remove, flagging 25 trades
      including all ten whose entry bars were already correct. Matcher
      fixed (entry on/after pick, exit on/after entry, closest match not
      first); it now reports 0 backdated entries and reproduces 8 of the
      10 known-good windows exactly.
    shipped:
      - "_build_benchmark_block reads the verified-window figures
        (alpha, portfolio leg, index leg, t-stat, trade count) with a
        labelled fallback to the blend when no verified windows exist"
      - "benchmark_scores drops the NASDAQ legacy fallback so all three
        index legs score the same trades; sweep now reads beating 0 of 3"
      - "recompute matcher: entry confined to bars on/after the pick,
        exit to bars on/after the entry, closest price wins over first"
      - 9 new drift guards (TestSingleAlphaSource,
        TestRecomputeMatcherCannotBackdate) + one superseded guard re-pinned
        to the same-trade-set contract
    deferred:
      - "operator: adjudicate the 8 unreconciled trades, then run the
        corrected recompute --apply. NOT run here - it rewrites published
        performance numbers. Corrected dry run says alpha -3.35% across 42
        trades, beating 1 of 3 indices (TSX only)"
      - "operator: the 8 unreconciled trades include the record's best
        (+20.11% DLTR), second-best (+13.36% AMD) and worst (-11.80% MDA)
        - the extremes are exactly what cannot be matched to market data"
      - "confidence is a dead field: 48 of 50 closed trades are Medium, 2
        Low, ZERO High. It informs sizing and the rule scoreboard while
        carrying no information. Make it calibrated or stop presenting it"
      - "exit timing is an artifact, not a decision: verified holds ran
        0-6 days (median 3) against a five-day thesis because exits are
        pinned to the Friday pre-market run. Per-trade alpha currently
        measures pick quality and an arbitrary exit date together"
      - "carried: significance-qualifier accept-or-abandon; SnapTrade
        Phase-0; closing-price lesson retirement"
    predictions:
      - metric: alpha figures spoken on air that come from the blended
          window (either block)
        baseline: "2 of 3 (Ep138 mislabelled, Ep139 blended, Ep140 correct)"
        expected: "0 of the next 10 - both blocks now read one source, so
          a blended number reaching air means a THIRD call site exists"
        verdict: pending
        evidence: null
      - metric: index-sweep legs scored from different trade counts
        baseline: "NASDAQ 45 vs S&P/TSX 10; sweep claimed beating 1 of 3"
        expected: "0 - all legs share one trade set every episode"
        verdict: pending
        evidence: null
      - metric: median script words, last-10 snapshot (grok-4.6 readout)
        baseline: "2078w over the first 3 episodes"
        expected: ">=1800w sustained at the 08-29 readout with zero
          numbers or tickers absent from the digest"
        verdict: pending
        evidence: null
      - metric: trades whose entry bar predates their pick date after a
          recompute dry run
        baseline: "25 of 50 under the first-match matcher"
        expected: "0 - and the 10 pick-date-aligned trades reproduce
          unchanged, which is the validation that matters"
        verdict: pending
        evidence: null
    agent_cost_usd: null

  - date: 2026-08-18b
    pr: null
    doc: docs/reviews/modern_investing_review_2026_08_18b.md
    reviewer: claude-code (operator-directed rules-based restart)
    summary: >
      Operator asked to start fresh alpha tracking from today's show with
      clear and reproducible entry/exit decisions, a working recursive
      learning loop, and continuous learning for the show and the
      audience. Root cause of five months of uninterpretable alpha was
      that the EXIT was never a decision - weekly holds closed on
      whichever bar the Friday pre-market run priced (Thursday's), so a
      Monday pick got five sessions and a Wednesday pick got one, and
      per-trade alpha measured pick quality and pick weekday together.
      Shipped a rulebook the code reads, a fixed session horizon, an era
      boundary so the on-air record starts today, one shared alpha
      selector, graded confidence, and a published methodology page.
    shipped:
      - "shows/_trading_policy.yaml - the rules as a file the code reads
        (load_policy / horizon_sessions / era_inception); entry at the
        first session open on/after the pick, exit at the stop or a fixed
        5-session horizon (flash 1), no discretionary exits"
      - "_pick_weekly_bars returns window[horizon-1] not window[-1], and
        _evaluate_open_trade counts printed sessions instead of asking
        whether today is Friday - every weekday pick now gets the same
        holding period"
      - "a fetch failure returns 0 sessions and reads as NOT due, so a bad
        network day can no longer close every open position; _close_trade
        takes the caller's bars so the exit decision and the exit pricing
        read one snapshot"
      - "execution/shadow.py derives its exit calendar from the same
        policy, guarded so sim and shadow cannot drift"
      - "era_* summary fields; the on-air record is trades picked on/after
        2026-08-18. Earlier trades stay published as history, are never
        blended, and lifetime totals are labelled HISTORY ONLY in the
        prompt while the era is empty"
      - "_alpha_scope() - ONE selector (era -> verified -> blended) used by
        both prompt blocks, each scope carrying its own label and trade
        count; direct fix for the Aug 15 two-source fusion"
      - "picks record Invalidation (the observable condition that would
        prove the thesis wrong), policy_version and horizon_sessions;
        confidence extraction accepts both label forms"
      - "get_mit_confidence_calibration is era-scoped and carries a rubric;
        a SECOND calibration function written during this pass was deleted
        before commit rather than shipping two sources of one number"
      - "docs/mit_trading_method.md - public methodology and an audit
        checklist for any track record; podcast prompt requires the rules
        and the invalidation line on air"
      - "6 method-literacy segments added to the existing evergreen library
        (invalidation, fixed horizons, what alpha measures, sample size,
        calibration, auditing a record) using its existing cooldowns"
      - 12 new drift guards (TestTradingPolicy, TestEraScopedRecord,
        TestReproducibleDecisions) + 5 superseded guards re-pinned
    deferred:
      - "operator: the 8 pre-era trades that reconcile to no price bar
        (incl. the record's best, second-best and worst) still need
        adjudication, then the corrected recompute --apply. They no longer
        contaminate the on-air number, so this is now history cleanup
        rather than a live integrity problem"
      - "the headline goes to zero for about a week and then to a sample
        too small to claim an edge - the prompts require saying so"
      - "carried: significance-qualifier accept-or-abandon; SnapTrade
        Phase-0; closing-price lesson retirement"
    predictions:
      - metric: era trades whose hold is not exactly the policy horizon
          (or an earlier stop-out)
        baseline: "pre-era holds ran 0-6 sessions, median 3"
        expected: "0 - every weekly hold spans exactly 5 sessions unless
          stopped out, whichever weekday it was picked"
        verdict: pending
        evidence: null
      - metric: alpha figures spoken on air sourced from pre-era trades
        baseline: "2 of 3 episodes on 2026-08-15/16 quoted the blend"
        expected: "0 - the era record or an explicit no-trades-yet
          statement, never a lifetime number"
        verdict: pending
        evidence: null
      - metric: distinct confidence ratings used across the era's picks
        baseline: "48 Medium / 2 Low / 0 High across 50 pre-era trades"
        expected: ">=2 distinct ratings within 10 picks, and no single
          rating above 90%"
        verdict: pending
        evidence: null
      - metric: picks carrying an Invalidation line
        baseline: "0 - the field did not exist"
        expected: ">=90% of picks, each naming a checkable condition
          rather than a direction"
        verdict: pending
        evidence: null
      - metric: sim vs shadow exit-date disagreements
        baseline: "n/a - different rules by construction"
        expected: "0 - both derive the horizon from the policy file"
        verdict: pending
        evidence: null
    agent_cost_usd: null

  - date: 2026-08-19
    pr: null
    doc: docs/reviews/modern_investing_review_2026_08_19.md
    reviewer: claude-code (operator audit — learning loop, options, transparency)
    summary: >
      Three operator questions, all checked against data. (1) LEARNING:
      the loop ran but its verdicts were artifacts - the rule scoreboard
      emitted FIVE identical RETIREMENT CANDIDATE findings (same 10
      trades, same -0.17% vs +0.43%) because all five rules were stamped
      on exactly the same trades and are perfectly collinear, while the
      control group was the pre-era trades whose windows the integrity
      passes had disowned. Six of thirteen active "rules" are production
      hygiene (re-teach cooldowns, "state the NASDAQ level", and three
      variants of "verify price data from multiple providers" - the
      pipeline's own data-fetch bugs taught as investing wisdom), and
      three more are the same volume rule in different scopes, scoring
      0.51-0.61 against a 0.62 dedup threshold. No rule had ever been
      scored or retired. (2) OPTIONS: 32 of the last 40 episodes discuss
      options and covered_call has been taught 33 times, against 0 of 61
      trades using any derivative. (3) TRANSPARENCY: solid aggregates, but
      the performance page showed five trades and nothing published
      carried entry/exit bar dates, stops, horizons, invalidations, rules
      in effect or confidence - none of what a listener needs to check the
      record or learn the process.
    shipped:
      - "rule scoreboard is era-scoped, refuses a verdict when every
        stamped trade carried the same rule set, flags collinear rules as
        ONE piece of evidence, requires >=5 trades on BOTH arms, and says
        'not measurable yet' rather than going silent"
      - "_is_trading_rule classifies production-hygiene lessons out of the
        pick prompt and the scoreboard (retires the long-carried LL-054
        closing-price item automatically)"
      - "_rule_core dedups on the rule's CONSTRAINT with its scope clause
        stripped - collapses LL-017 into LL-067 at 0.796 and admits the
        sector-cap rule instead"
      - "options positions: covered calls and cash-secured puts held to
        expiry. Contract quoted LIVE at pick time (real strike/expiry/
        bid-ask mid); payoff at expiry is arithmetic on the underlying
        close with no free parameters, verified against hand calculations"
      - "deterministic selection - nearest listed expiry 21-45d out,
        listed strike closest to 4% OTM, ITM and unquoted strikes skipped"
      - "if the chain cannot be quoted the pick degrades to shares and
        records option_quote_failed; a premium is NEVER estimated"
      - "settlement requires evidence the market traded on/after expiry (a
        test caught this settling a fresh position against its own entry
        day)"
      - "shows/_trading_policy.yaml gains the options rulebook; both
        prompts require the structure, its mechanics and the
        early-assignment caveat on air"
      - "scripts/build_mit_ledger.py -> api/mit_trade_ledger.json + .csv,
        published nightly: every trade with the full decision record,
        including the 7 voided picks and the pre-era trades, flagged
        rather than hidden"
      - 20 new drift guards (TestOptionsPositions, TestRuleScoreboardHonesty,
        TestTradingVsPipelineRules, TestPublicLedger) + 4 superseded guards
        re-pinned with reasons
    deferred:
      - "per-strategy performance: the 61 strategy strings are unique
        free-text, so there is nothing to group on. Tagging them into
        families is the next tractable step and would let the show report
        which APPROACHES work, not just which sectors"
      - "the loop has still produced zero scored rules - it is now capable
        and refuses to fake it, but a first verdict needs the rule set to
        vary across roughly a dozen era trades"
      - "operator: the 8 pre-era trades reconciling to no market bar still
        need adjudication (now history cleanup, not a live problem)"
      - "carried: significance-qualifier accept-or-abandon; SnapTrade
        Phase-0"
    predictions:
      - metric: rule-effectiveness verdicts emitted from a single
          undivided sample
        baseline: "5 identical RETIREMENT CANDIDATE flags from one 10-trade set"
        expected: "0 - the scoreboard reports 'not measurable' until the
          rule set genuinely varies"
        verdict: pending
        evidence: null
      - metric: production-hygiene rules appearing in the pick prompt
        baseline: "6 of 13 active rules"
        expected: "0 - only trading rules are stamped and scored"
        verdict: pending
        evidence: null
      - metric: simulated trades using an options structure
        baseline: "0 of 61, against options taught in 32 of 40 episodes"
        expected: ">=1 within 20 episodes, with a real recorded premium
          and an expiry-settled payoff; ZERO positions carrying an
          estimated premium"
        verdict: pending
        evidence: null
      - metric: trades published with their full decision record
        baseline: "5 rows on the performance page, no entry/exit dates,
          stops, horizons or invalidations anywhere public"
        expected: "100% of trades in api/mit_trade_ledger.json, voided
          ones included"
        verdict: pending
        evidence: null
    agent_cost_usd: null

  - date: 2026-08-20
    pr: null
    doc: docs/reviews/modern_investing_review_2026_08_20.md
    reviewer: claude-code (operator question — further improvements)
    summary: >
      Audit for remaining improvements found four; three shipped. (1) The
      learning loop was honest but POWERLESS - across 15 stamped trades
      only two rule sets ever existed, differing by one rule and only
      because the ledger happened to change. Four of five rules were
      common to both, so no rule could ever be told apart from another and
      the scoreboard would have stayed silent indefinitely while looking
      like a working feedback loop. (2) 61 trades produced 61 UNIQUE
      free-text strategy strings, so the show could report alpha by sector
      but never by APPROACH - "are our momentum entries better than our
      valuation screens" was unanswerable. (3) The daily audit runs at a
      fixed 16:15 UTC while shows finish anywhere from 09:32 to 19:41; a
      late episode was logged "critical: Missed episode" and auto-closed
      next day by confirming the FILE EXISTS, so its content was never
      reviewed. On 2026-08-19 that hit four shows at once, and the first
      catch-up run found a 32-episode network-wide backlog.
    shipped:
      - "deterministic rule rotation (shows/_trading_policy.yaml
        learning.rule_rotation) - 4 slots from a ~6-rule pool cycling by
        episode number, giving every rule both a with-rule and a
        without-rule arm; reproducible from the trade record"
      - "proven rules are pinned and stop rotating; always_on: true pins
        by hand for safety-shaped rules"
      - "_proven_rule_ids scores a rule as proven only with >=5 trades on
        both arms and a positive difference"
      - "closed strategy-family vocabulary (10 families) REQUIRED on new
        picks and DERIVED from free text for the 61 historic trades, so
        per-approach alpha is reportable immediately"
      - "_build_strategy_family_performance feeds the pick prompt and
        inherits the window discipline - verified-window trades when there
        are enough, otherwise labelled indicative-only and not for air"
      - "review_episodes.py catch-up pass: episodes from the previous 3
        days with no coverage mark are reviewed, tracked in
        api/review_coverage.json (14-day prune), capped at 10 per run
        oldest-first so the AI review cost never spikes"
      - "daily-audit.yml commits api/review_coverage.json so the pass is
        idempotent across CI runs"
      - 18 new drift guards (TestRuleRotation, TestStrategyFamilies,
        TestReviewCatchUp)
    deferred:
      - "LISTENER FEEDBACK LOOP - the biggest remaining gap. The system
        learns from its trades, its rules, YouTube retention and an LLM
        judge, but has NO path for a listener's experience to enter the
        loop (zero mentions of reply/email/question in the podcast
        prompt) despite running a newsletter that could carry one. For a
        show that exists to teach investing, the one signal it never
        collects is whether anyone learned anything. Needs an inbox and a
        triage path — operator decision, not just code"
      - "long-form YouTube retention 10% vs 33% on Shorts across 169
        videos; long-form is where subscribers convert, but this is a
        network-wide video problem"
      - "audience flat at 221 downloads/30d (weekly 52/44/61/50)"
      - "carried: the 8 pre-era trades reconciling to no market bar;
        significance-qualifier accept-or-abandon; SnapTrade Phase-0"
    predictions:
      - metric: distinct rule sets stamped across era trades
        baseline: "2 sets across 15 trades, differing by one rule, by accident"
        expected: ">=4 distinct sets within 20 era trades, and every
          active rule carrying both a with-rule and a without-rule arm"
        verdict: pending
        evidence: null
      - metric: rules with enough evidence to be scored (>=5 trades on
          both arms)
        baseline: "0 - structurally impossible before rotation"
        expected: ">=1 scored rule within ~25 era trades; if still 0 by
          then, the pool is too small and needs widening rather than more
          waiting"
        verdict: pending
        evidence: null
      - metric: picks carrying an explicit strategy family
        baseline: "0 - the field did not exist; 61 unique free-text strings"
        expected: ">=90% of new picks, and a per-family record the show
          can quote once any family reaches 5 verified-window trades"
        verdict: pending
        evidence: null
      - metric: episodes that finish after the audit window and are never
          content-reviewed
        baseline: "4 on 2026-08-19 alone; a 32-episode backlog at first run"
        expected: "0 - the catch-up pass reviews them within 3 days"
        verdict: pending
        evidence: null
    agent_cost_usd: null

  - date: 2026-08-20b
    pr: null
    doc: docs/reviews/modern_investing_review_2026_08_20b.md
    reviewer: claude-code (operator question - announce the changes on air)
    summary: >
      Checked what Ep141-143 actually aired after the record restarted.
      All three announced the new regime; one of three said WHY it
      restarted; none pointed at the rulebook or the ledger; and none
      mentioned the figure the show used to quote. From a listener's
      seat the old "+9.28% across forty-five trades" simply vanished -
      which is indistinguishable from burying a bad result, when the
      truth is the opposite (it could not be reproduced, so it was not
      ours to claim). Shipped a NARROW one-off correction rather than a
      recurring changelog, and made the "published for anyone to check"
      claim actually true - the ledger was built nightly into api/ and
      linked from nowhere.
    shipped:
      - "_build_methodology_disclosure - a self-retiring on-air segment
        (METHODOLOGY_DISCLOSURE_EPISODES = 3) covering why the old figure
        went, the weekday-dependent exit that was the deeper problem, the
        pre-era trades that match no bars, the published rulebook, the
        honest cost of a small record, and how to audit ANY track record"
      - "stamped on the tracker as a list of episode numbers, so a second
        get_prompt_context call in one episode cannot consume two airings
        and the record shows which episodes carried it"
      - "read-only (test/rehearse) runs never consume an airing"
      - "internals are deliberately out of scope - rule rotation, review
        coverage and scoreboard mechanics are not listener content, and a
        drift guard fails if they leak into the spoken portion"
      - "performance page gained a Verify this record yourself panel
        linking the rulebook and the trade ledger (JSON + CSV); a guard
        asserts both the links and the files exist"
    predictions:
      - metric: episodes carrying the methodology correction
        baseline: "0 - Ep141-143 announced the new regime but never
          explained the disappearance of the old figure"
        expected: "exactly 3, then 0 forever; if a 4th airs the
          self-retirement is broken"
        verdict: pending
        evidence: null
      - metric: listener-value score on the correction episodes
        baseline: "~7.3 steady state since Ep128"
        expected: "no regression below 7.0 - the segment displaces
          roughly 250 words of regular content, and if it costs more than
          it teaches it should not have aired 3 times"
        verdict: pending
        evidence: null
      - metric: visits to modern-investing-performance.html in the 14
          days after the correction airs
        baseline: "unmeasured - the page existed but the show never
          pointed at the primary sources"
        expected: "measurable and non-zero; if it stays flat the on-air
          pointer is not working and the ask needs rewording, not
          repeating"
        verdict: pending
        evidence: null
    agent_cost_usd: null
  - date: 2026-09-05
    pr: null
    doc: docs/reviews/network_delivery_review_2026_09_05.md
    summary: "Network delivery review sample - the worst offender. Ep160 read the full portfolio record twice (Market Pulse REQUIRED the alpha 'in the same breath' while Portfolio Performance was 'the ONLY segment'); two full teaching segments per episode taught the same mechanic; 'volume above the 20-day average' x6; 'STORYTELLING over lecturing' and the sports-season NASDAQ arc were prompt text; Ep159 spoke the gallery plug twice around the disclosure."
    shipped:
      - "modern_investing_podcast.txt - Market Pulse speaks NASDAQ level + YTD only, portfolio numbers only in Portfolio Performance; one mechanic taught once; storytelling / sports-season / specimen lines -> worked-example-over-abstraction (A/B)"
      - "modern_investing_digest.txt - 'build narrative tension, make the NASDAQ race feel alive' -> reported plainly once with trade count (A/B)"
      - "shared CONTENT DISCIPLINE block (A/B)"
      - "run_show._collapse_duplicate_tail_lines (doubled gallery plug)"
      - "closing pinned to one sign-off (A/B)"
    deferred:
      - "none"
    predictions:
    - metric: "episodes reading cumulative P&L / alpha in two segments (manual)"
      baseline: "2/2 (Ep159/160)"
      expected: "0/10"
      verdict: pending
      evidence: null
    - metric: "script_repeated_facts per episode"
      baseline: "3-4 (Ep157-160)"
      expected: "<=1"
      verdict: pending
      evidence: null
    - metric: "a mechanic taught in two segments of one episode (manual)"
      baseline: "2/2"
      expected: "0/10"
      verdict: pending
      evidence: null
    - metric: "duplicate tail line around the disclosure"
      baseline: "1/2 (Ep159)"
      expected: "0/10"
      verdict: pending
      evidence: null
    agent_cost_usd: null
  - date: 2026-09-06
    pr: null
    doc: docs/reviews/network_delivery_review_2026_09_05.md
    summary: "Sep 6 readout (Ep162 vs Ep161): clearly better - zero schema-field leakage (Ep161 read 'Trade Type: Weekly Hold.' aloud), portfolio record spoken ONCE, density 2.9->3.2/100w - but three prompt-supplied lines aired verbatim ('cost me money before I figured it out'; 'what this teaches us'; 'is exactly how you would check'), volume confirmation was explained in five segments, and the hook aired 'oil slips below USninety-five dollars' (US$ had no pronunciation handler)."
    shipped:
      - "modern_investing_podcast.txt - three quotable lines de-seeded by shape; ONE lesson has ONE owner (A/B)"
      - "assets/pronunciation replace_canadian_currency handles US$ / USD$ -> 'N U S dollars'"
    predictions:
    - metric: "verbatim airing of any of the three de-seeded lines, next 10 episodes"
      baseline: "3/3 on Ep162"
      expected: "0"
      verdict: pending
      evidence: null
    - metric: "segments explaining the same mechanic (hand-count), 3 episodes"
      baseline: "5 (volume confirmation, Ep162)"
      expected: "<=2"
      verdict: pending
      evidence: null
    - metric: "'US' glued to a number word in any _tts.txt"
      baseline: "1 (Ep162 hook)"
      expected: "0"
      verdict: pending
      evidence: null
    agent_cost_usd: null

  - date: '2026-09-09'
    pr: null
    doc: docs/reviews/modern_investing_review_2026_09_09.md
    generated_by: grok-4.5
    summary: Grok did not return the JSON envelope; raw output kept.
    scored_prior_predictions: []
    shipped: []
    deferred: []
    predictions: []
    proposed_changes_in_pr: []
    agent_cost_usd: 0.1866
do_not_retry: []
